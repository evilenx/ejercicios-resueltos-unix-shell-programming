if [ $# -eq 0 ]; then
  echo "Usage: invalid varname1 varname2 ... varnamen"
  exit 1
fi

while [ $# -gt 0 ]; do
  echo $1 | grep "^[_A-Za-z]?*[_A-Za-z0-9]*$" >> /dev/null
  if [ $? -eq 0 ]; then
    echo $1" VALID."
  else
    echo $1" INVALID."
  fi
  shift
done
exit 0

